package org.springscala.configuration

import org.springframework.batch.item.file.mapping.FieldSetMapper
import org.springframework.batch.item.file.transform.FieldSet
import org.springscala.domain.Employee

class EmployeeMapper //extends FieldSetMapper[Employee]
{
   // override def mapFieldSet(fieldSet: FieldSet): Employee    = {
      //val emp = new Employee(id = fieldSet.readString("id"), firstName =fieldSet.readString("firstName"), lastName = fieldSet.readString("lastName") )
     // emp
    //}
}
